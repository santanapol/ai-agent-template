# Server
PORT=3015
TZ=UTC
NODE_ENV=development

# Gateway mesh — must match gateway `GATEWAY_SECRET`
GATEWAY_SECRET=change-me-in-dev

# MongoDB (branch shared DB — production: Atlas read replica + MONGODB_DB_BRANCH=gpp_777ww)
MONGODB_URI=mongodb://localhost:27017
MONGODB_DB_BRANCH=gpp_777ww
