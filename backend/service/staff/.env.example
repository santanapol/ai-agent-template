# =============================================================================
# staff-service — environment example
# =============================================================================
# Which file to edit?  backend/ENV.md
# Manual dev: cp .env.example → .env  |  Harness: dev-up uses .env.harness (see backend/ENV.md)

# -----------------------------------------------------------------------------
# Server
# -----------------------------------------------------------------------------
PORT=3101
TZ=UTC
NODE_ENV=development
APP_NAME=staff-service

# -----------------------------------------------------------------------------
# Gateway / Internal Trust
# -----------------------------------------------------------------------------
GATEWAY_SHARED_SECRET=test-gateway-secret-32-chars-minimum!!

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------
# MongoDB (shared with auth — harness ใช้ zero-platform_0)
MONGODB_URI=mongodb://127.0.0.1:27017/zero-platform
DB_NAME=zero-platform

# -----------------------------------------------------------------------------
# Auth Integration
# -----------------------------------------------------------------------------
# Outbound auth internal (T11+)
AUTH_INTERNAL_BASE_URL=http://127.0.0.1:3001
AUTH_INTERNAL_SERVICE_SECRET=staff-internal-secret-32-chars-min!!
STAFF_PROVISION_DEFAULT_ROLE=staff

# Archive revoke retry (T14) — exponential backoff base delay
AUTH_REVOKE_MAX_RETRIES=3
AUTH_REVOKE_BACKOFF_MS=200
METRICS_ENABLED=false

# -----------------------------------------------------------------------------
# Optional tuning
# -----------------------------------------------------------------------------
# LOG_LEVEL=info
# LOG_PRETTY=true
# SHUTDOWN_TIMEOUT_MS=10000

# -----------------------------------------------------------------------------
# Dynamic Permission
# -----------------------------------------------------------------------------
PERMISSION_MODE=dual
