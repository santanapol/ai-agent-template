# =============================================================================
# staff-service — harness environment example
# =============================================================================
# dev-up loads .env.harness (gitignored). First time: cp .env.harness.example .env.harness
# Put Atlas / read URIs in .env.harness — dev-up only refreshes port/DB/route keys.
# Guide: backend/ENV.md

# -----------------------------------------------------------------------------
# Server
# -----------------------------------------------------------------------------
PORT=3101
TZ=UTC
NODE_ENV=development
APP_NAME=staff-service

# -----------------------------------------------------------------------------
# Gateway / Internal Trust
# -----------------------------------------------------------------------------
GATEWAY_SHARED_SECRET=test-gateway-secret-32-chars-minimum!!

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------
# MongoDB (shared with auth in dev — ใช้ DB_NAME / URI เดียวกับ auth)
MONGODB_URI=mongodb://127.0.0.1:27017/auth_login
DB_NAME=auth_login

# -----------------------------------------------------------------------------
# Auth Integration
# -----------------------------------------------------------------------------
# Outbound auth internal (T11+)
AUTH_INTERNAL_BASE_URL=http://127.0.0.1:3001
AUTH_INTERNAL_SERVICE_SECRET=staff-internal-secret-32-chars-min!!
STAFF_PROVISION_DEFAULT_ROLE=staff

# Archive revoke retry (T14) — exponential backoff base delay
AUTH_REVOKE_MAX_RETRIES=3
AUTH_REVOKE_BACKOFF_MS=200
METRICS_ENABLED=false

# -----------------------------------------------------------------------------
# Optional tuning
# -----------------------------------------------------------------------------
# LOG_LEVEL=info
# LOG_PRETTY=true
# SHUTDOWN_TIMEOUT_MS=10000

# -----------------------------------------------------------------------------
# Dynamic Permission
# -----------------------------------------------------------------------------
PERMISSION_MODE=dual

