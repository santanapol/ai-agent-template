{
  "name": "gateway",
  "version": "0.2.4",
  "private": true,
  "description": "API Gateway — verify JWT (JWKS), inject trusted headers, proxy to upstreams",
  "type": "module",
  "engines": {
    "node": ">=24 <25"
  },
  "packageManager": "npm@11.12.1",
  "scripts": {
    "start": "node --env-file=.env --enable-source-maps src/server.js",
    "dev": "node --env-file=.env --enable-source-maps --watch src/server.js",
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "spec:lint": "spectral lint openapi.yaml --ruleset .spectral.yaml --fail-severity=error",
    "spec:codes": "node scripts/validate-gateway-openapi-problem-codes.mjs",
    "spec:consistency": "node scripts/validate-spec-consistency.mjs",
    "audit:check": "npm audit --audit-level=high",
    "ci": "npm run lint && npm run format:check && npm run spec:lint && npm run spec:codes && npm run spec:consistency && npm test && npm run audit:check",
    "test": "node --enable-source-maps --test --test-timeout=120000 test/*.test.js test/*/*.test.js",
    "test:coverage": "c8 --reporter=text --reporter=html node --test --test-timeout=120000 test/**/*.test.js",
    "dev:upstream": "node scripts/dev-upstream.mjs",
    "try:proxy": "node scripts/try-proxy.mjs"
  },
  "dependencies": {
    "@zero-platform/roles": "file:../shared/platform-roles",
    "@fastify/cors": "^11.2.0",
    "@fastify/http-proxy": "^11.4.4",
    "fastify": "^5.8.5",
    "fastify-plugin": "^5.1.0",
    "jose": "^5.10.0",
    "pino-pretty": "^13.1.3",
    "redis": "^4.7.1"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.4",
    "@stoplight/spectral-cli": "^6.15.0",
    "c8": "^11.0.0",
    "eslint": "^9.39.4",
    "globals": "^15.15.0",
    "prettier": "^3.5.3"
  }
}
