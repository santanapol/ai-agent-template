# Dependencies
node_modules/

# Local env (never commit secrets)
.env
.env.*
!.env.example
!**/.env.example
!.env.test
!**/.env.test
!.env.defaults
!**/.env.defaults
!.env.harness.example
!**/.env.harness.example
!**/.env.staging.example
!**/.env.local.example

# Test / tooling
coverage/
.nyc_output/
test-results/
playwright-report/

# Logs & OS
*.log
.DS_Store
Thumbs.db

# Build / cache
dist/
dist-ssr/
.cache/
.vite/
*.tsbuildinfo

# Cursor local config (may contain MCP tokens)
.cursor/mcp.json

# Bruno local environments (copy from Local.yml.example; never commit tokens/passwords)
**/environments/Local.yml
**/environments/*.local.yml
!**/environments/Local.yml.example
!**/environments/*.example.yml

# Harness dev runtime (logs, pids only; offset≠0 also has harness/)
.dev-run/

# Server credentials (SSH passwords, staging logins — never commit)
server-environment/**/credential.md

# Local studio-admin design reference (full upstream clone — not committed)
coding-standard/frontend/backoffice/reference/studio-admin/
