# Error catalogue (as-built)

**as-built-of:** {as-built-of}  
Envelope shape: {describe success/error envelope from code}

| Code | Meaning | Actor(s) | HTTP (approx) |
|------|---------|----------|---------------|
| | | | |

## Warnings (non-error)

| Code | Meaning | Actor(s) |
|------|---------|----------|
| | | |

## Notes

- Prefer asserting `error.code` + HTTP over long `message` strings when copy may change.
- UI mapping: see `ui/{actor}.md`.
