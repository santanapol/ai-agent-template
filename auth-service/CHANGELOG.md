# Changelog

## 0.1.0

- Initial `auth-service` implementation aligned with `auth-login-design.md` (login, opaque refresh handling, access JWT RS256, rate limits, security constraints per SoT).
- Minimal `docs/openapi.yaml` and `.env.example`; scripts for dev env and seed data.
