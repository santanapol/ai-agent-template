# Deploy v0.8.4 — 2026-07-15 (staging)

## Version

| | |
|---|---|
| **Platform** | `v0.8.4` |
| **Git tag** | `v0.8.4` (after staging smoke) |
| **Merge commit** | _(fill after merge)_ |
| **Previous tag** | `v0.8.3` → `4c1a0b7` |

## สรุป

`ListPageCard`'s default `descriptionClassName` (`max-w-sm`) was too narrow for Channel Performance's longer description, causing it to wrap. Added `descriptionClassName="max-w-2xl"` matching the same override already used by `SmartReportList.tsx`. UI-only, no API/schema/env change.

## Services ที่กระทบ

| PM2 app | Package / path | เหตุผล |
|---------|----------------|--------|
| `zero-backoffice` | `frontend/backoffice-next` | `ChannelPerformancePage.tsx` — 1-line className override |

Backend ทุก service ไม่กระทบ — ไม่ต้อง reload

## Human ops

ไม่มี

## ก่อน deploy (staging server)

- [ ] `bash scripts/staging/staging-verify-env.sh`
- [ ] บันทึก `PREVIOUS_GOOD_SHA` (ก่อน pull)

## Deploy

```bash
cd /var/www/zero-platform
PREVIOUS_GOOD_SHA=$(git rev-parse HEAD)
git pull origin main
bash scripts/staging/staging-verify-env.sh
bash scripts/staging/deploy-staging.sh
```

## หลัง deploy — smoke

```bash
SMOKE_PASSWORD='…' STAGING_URL=https://zero-staging.168bits.com bash scripts/staging/smoke-staging.sh
```

**Manual UAT แนะนำ:**

- Channel Performance → description ใต้หัวข้อแสดงบรรทัดเดียว

## Post-smoke — git tag

```bash
git fetch origin main && git checkout main && git pull
./scripts/release/release-tag.sh v0.8.4
git push origin v0.8.4
```

- [ ] Smoke ผ่าน
- [ ] Tag `v0.8.4` pushed

## Rollback

```bash
cd /var/www/zero-platform
git fetch origin && git reset --hard v0.8.3
bash scripts/staging/deploy-staging.sh
```

## Breaking / migration

ไม่มี — className เปลี่ยนอย่างเดียว
