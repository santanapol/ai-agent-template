# Deploy v0.7.0 — 2026-07-13 (staging)

## Version

| | |
|---|---|
| **Platform** | `v0.7.0` |
| **Git tag** | `v0.7.0` → `db71e0d` (pushed 2026-07-13) |
| **PRs** | [#64](https://github.com/Chiang-Rai-Technology/zero-platform/pull/64)–[#70](https://github.com/Chiang-Rai-Technology/zero-platform/pull/70) |
| **Merge commit** | `db71e0d` (tagged `v0.7.0`; includes post-release PM2 memory cap fix) |
| **Previous tag** | `v0.6.0` |

## สรุป

Deferred TD closeout: batch invoice API + FE bulk export, Smart Report drawer server pagination, smart-report schedule index (code), CI baseline validator sync, orphan fee ops runbook. Target: **staging** (`zero-staging.168bits.com`).

**Prerequisite:** `/ship` GO on merged PRs #64–#69; main CI green at `b77afbe`.

## Services ที่กระทบ

| PM2 app | Package / path | เหตุผล |
|---------|----------------|--------|
| `zero-backoffice` | `frontend/backoffice-next` | bulk export batch client; Smart Report drawer pagination |
| `zero-agent-invoice` | `backend/service/agent-invoice` | `GET /api/v1/invoices/batch` (OpenAPI 1.2.0) |
| `zero-smart-report` | `backend/service/smart-report` | `IDX_REPORTS_SCHEDULE_FREQUENCY` in init-db (index on DB = human ops) |

Gateway/auth/staff — ไม่มี breaking env; reload ตาม `deploy-staging.sh` ปกติ

## Human ops (หลัง deploy code — ไม่บล็อก smoke แต่ต้องทำก่อน prod)

| TD | Action | Doc |
|----|--------|-----|
| TD-027 | `createIndex` schedule.frequency บน staging → prod | [smart-report-schedule-frequency-index-2026-07-13](../ops/smart-report-schedule-frequency-index-2026-07-13.md) |
| TD-018 | audit → staging dry-run → prod delete orphan fee | [agent-fees-orphan-cleanup-2026-07-13](../ops/agent-fees-orphan-cleanup-2026-07-13.md) |

ปิด TD-018 ใน tech-debt tracker หลัง prod delete confirm

## ก่อน deploy (staging server)

- [ ] `bash scripts/staging/staging-verify-env.sh`
- [ ] บันทึก `PREVIOUS_GOOD_SHA` (ก่อน pull)
- [ ] ไม่มี env ใหม่ — [backend/ENV.md](../../backend/ENV.md)

## Deploy

```bash
cd /var/www/zero-platform
PREVIOUS_GOOD_SHA=$(git rev-parse HEAD)
git pull origin main
bash scripts/staging/staging-verify-env.sh
bash scripts/staging/deploy-staging.sh
```

## หลัง deploy — smoke

```bash
SMOKE_PASSWORD='…' bash scripts/staging/smoke-staging.sh
```

จากเครื่อง dev:

```bash
SMOKE_PASSWORD='…' STAGING_URL=https://zero-staging.168bits.com bash scripts/staging/smoke-staging.sh
```

**Manual UAT แนะนำ:**

- Invoice bulk export 5–10 ใบ → Network = 1× `GET .../invoices/batch`
- Smart Report drawer: history >20 → เปลี่ยนหน้า → request `page=2`
- Regression: list/filter Smart Report, invoice list ยังปกติ

## Post-smoke — git tag

```bash
git fetch origin main && git checkout main && git pull
./scripts/release/release-tag.sh v0.7.0
git push origin v0.7.0
```

- [x] Smoke ผ่าน (2026-07-13)
- [x] Tag `v0.7.0` pushed → `db71e0d`

## Rollback

```bash
cd /var/www/zero-platform
git fetch origin && git reset --hard v0.6.0   # or PREVIOUS_GOOD_SHA
bash scripts/staging/deploy-staging.sh
```

## Breaking / migration

| เปลี่ยน | การแก้ |
|--------|--------|
| ไม่มี breaking API สำหรับ consumer เดิม | batch endpoint เพิ่มใหม่; detail/transaction routes เดิมยังใช้ได้ |
| Index TD-027 | code deploy ไม่สร้าง index บน prod อัตโนมัติ — ตาม ops doc |
