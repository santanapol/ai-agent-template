# Deploy v0.6.0 — 2026-07-13 (staging)

## Version

| | |
|---|---|
| **Platform** | `v0.6.0` |
| **Git tag** | `v0.6.0` → `f650c84` (2026-07-13) |
| **PR** | [#59](https://github.com/Chiang-Rai-Technology/zero-platform/pull/59) |
| **Merge commit** | `f650c84` |
| **Staging deployed** | `f650c84` @ `zero-staging.168bits.com` (2026-07-13) |
| **Previous tag** | `v0.5.0` |

## สรุป

Backoffice UX/shadcn DS migration + Smart Report editor v2 + staff profile pages + backend สนับสนุน (staff optional contact, smart-report sandbox, invoice currency). Target: **staging** (`zero-staging.168bits.com`).

**Prerequisite:** `/ship` GO (lint/test/build + docs-lint ผ่านบน branch `fix/fe-ux-shadcn-ds-2026-07-10`).

## Services ที่กระทบ

| PM2 app (`ecosystem.staging.config.js`) | Package / path | เหตุผล |
|----------------------------------------|----------------|--------|
| `zero-backoffice` | `frontend/backoffice-next` | UX หลัก, routes ใหม่, 547 Vitest |
| `zero-smart-report` | `backend/service/smart-report` | sandbox projection, reports service |
| `zero-staff` | `backend/service/staff` | optional email/tel, profile PATCH |
| `zero-agent-invoice` | `backend/service/agent-invoice` | invoice detail currency, status update |

Gateway/auth — ไม่มี breaking env ในรอบนี้; reload ตาม `deploy-staging.sh` ปกติ

Scripts: `deploy-staging.sh`, `smoke-staging.sh`, `release-tag.sh`

## ก่อน deploy (staging server)

- [x] `bash scripts/staging/staging-verify-env.sh`
- [x] บันทึก `PREVIOUS_GOOD_SHA` (ก่อน pull: `7aed652`)
- [x] ไม่มี env ใหม่ — อ้างอิง [backend/ENV.md](../../backend/ENV.md) ถ้าสงสัย

## Deploy

```bash
cd /var/www/zero-platform
PREVIOUS_GOOD_SHA=$(git rev-parse HEAD)
git pull origin main
bash scripts/staging-verify-env.sh
bash scripts/deploy-staging.sh
```

`deploy-staging.sh` รัน `npm run build:staging` ใน `frontend/backoffice-next` และ `pm2 reload ecosystem.staging.config.js`

## หลัง deploy — smoke

```bash
SMOKE_PASSWORD='…' bash scripts/smoke-staging.sh
```

จากเครื่อง dev:

```bash
SMOKE_PASSWORD='…' STAGING_URL=https://zero-staging.168bits.com bash scripts/smoke-staging.sh
```

**Manual UAT แนะนำ (FE):**

- Smart Report: create → validate → test → save; list filters + history drawer
- Staff: create without email/tel; edit profile; permission-gated routes
- Invoice detail: export, back via list filters (`?return=`)
- Permissions: role save dialog; wildcard notice
- Branch switcher: inactive branch + Channel Performance re-search

## Post-smoke — git tag

```bash
git fetch origin main && git checkout main && git pull
./scripts/release/release-tag.sh v0.6.0 f650c84
git push origin v0.6.0
```

- [x] Smoke ผ่าน (`scripts/staging/smoke-staging.sh` จาก local, 2026-07-13)
- [x] Tag `v0.6.0` → `f650c84`

## Rollback

```bash
cd /var/www/zero-platform
git fetch origin && git reset --hard v0.5.0   # or PREVIOUS_GOOD_SHA
bash scripts/deploy-staging.sh
```

## Known / accepted (จาก /ship)

- Test debt: error-path coverage ใน `useSmartReportEditor` / `useInvoices` — fast-follow
- `StaffProfilePage.test.tsx` อาจ flake ใน parallel CI — ดู GHA `frontend-next-checks`
- Inactive branch switchable ใน UI — backend ต้อง enforce สิทธิ์ branch-scoped
- Biome warnings ~72 รายการ (style) — 0 lint errors

## Breaking / migration

| เปลี่ยน | การแก้ |
|--------|--------|
| Smart Report editor เป็น route แยก | bookmark `/smart-reports/new`, `/smart-reports/:id/edit` |
| Staff drawer → profile pages | ใช้ `/staff/new`, `/staff/:id/edit` |
| Legacy `frontend/backoffice` | deprecated ใน docs (directory ยังอยู่) — ใช้ `backoffice-next` เท่านั้น |
