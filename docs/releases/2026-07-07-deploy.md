# Deploy v0.4.0 — 2026-07-07 (staging)

## Version

| | |
|---|---|
| **Platform** | `v0.4.0` |
| **Git tag** | `v0.4.0` (annotated; push after smoke) |
| **Merge commit** | `53fbd493cfe1f71e9d98a74dd218208f6069afea` (PR #51) |
| **Previous tag** | `v0.3.0` |

## สรุป

Auth-owned branch list API, backoffice switcher migration, `.env.harness` harness refactor, env naming (`GATEWAY_SHARED_SECRET`, `MONGODB_URI_READ`), staging ops scripts, integration tests + smoke hardening. Target: **staging** only.

## Services ที่กระทบ

| PM2 app (`ecosystem.staging.config.js`) | Package |
|----------------------------------------|---------|
| `zero-auth` | `backend/auth` |
| `zero-gateway` | `backend/gateway` |
| `zero-staff` | `backend/service/staff` |
| `zero-agent-invoice` | `backend/service/agent-invoice` |
| `zero-smart-report` | `backend/service/smart-report` |
| `zero-branch-report` | `backend/service/branch-report` |
| *(frontend)* | `frontend/backoffice` — static `dist/` via Nginx |

Scripts: `dev-up`, `dev-generate-env`, `deploy-staging`, `setup-staging`, `staging-init-env`, `staging-seed-all`, `staging-verify-env`, `smoke-staging`, `release-tag`

## ก่อน merge / deploy (staging server)

- [ ] `bash scripts/staging-verify-env.sh` — `GATEWAY_SHARED_SECRET`, `MONGODB_URI_READ`, `branch-report PORT=3104`
- [ ] ยืนยัน `backend/*/.env.staging` ใช้ชื่อ env ใหม่ (ดู [backend/ENV.md](../../backend/ENV.md))
- [ ] อย่า commit `dev-ops/staging/credential.md` หรือ `.env.staging`
- [ ] บันทึก `PREVIOUS_GOOD_SHA` ก่อน `git pull`

## หลัง merge — deploy บน staging

```bash
cd /var/www/zero-platform
PREVIOUS_GOOD_SHA=$(git rev-parse HEAD)
git pull origin main
bash scripts/staging-verify-env.sh
bash scripts/deploy-staging.sh
```

## หลัง deploy — smoke

```bash
SMOKE_PASSWORD='…' bash scripts/smoke-staging.sh
```

- [x] Staging deploy + smoke (2026-07-07)
- [x] `git push origin v0.4.0` — tag on merge commit `53fbd49`

## Breaking / migration

| เปลี่ยน | การแก้ |
|--------|--------|
| Harness env | `backend/<service>/.env.harness` แทน `0/env/*` |
| `GATEWAY_SECRET` → `GATEWAY_SHARED_SECRET` | อัปเดต `.env.staging` บน server |
| branch-report `PORT` | ต้องเป็น `3104` |

## Rollback

```bash
cd /var/www/zero-platform
git fetch origin
git reset --hard v0.3.0   # or PREVIOUS_GOOD_SHA
bash scripts/deploy-staging.sh
```

**RTO:** ~5–10 นาที

## Acknowledged risks (staging)

- Staging อ่าน prod Atlas read replica สำหรับชื่อสาขาจริง
- smart-report VM sandbox — admin-only; pipeline guards in place

## References

- [dev-ops/staging/RUNBOOK.md](../../dev-ops/staging/RUNBOOK.md)
- [CHANGELOG.md](../../CHANGELOG.md)
