# code-base

Placeholder zone for **application code** in this template. Folders start empty — add your project after fork.

| Path | Put here |
|------|----------|
| `backend/` | API services, gateway, shared packages |
| `frontend/` | Web apps (e.g. backoffice) |

After fork, vendor org rules into [`coding-standard/`](../coding-standard/) and follow applicable paths when implementing.

Product specs and exec plans live in [`docs/`](../docs/), not under `code-base/`.

Start navigation at [AGENTS.md](../AGENTS.md).
