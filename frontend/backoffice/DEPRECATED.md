# Deprecated — use `frontend/backoffice-next`

This Vite + React Router app is **frozen**. New work happens in [`../backoffice-next`](../backoffice-next).

Deploy and harness (`dev-up.sh --with-frontend`) now target the Next.js app on port **3005**.

**Reference archive** — tree restored from git (`d3f4f67`) for code lookup only. Do not wire into CI, PM2, or nginx.
