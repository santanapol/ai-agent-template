# Staging build — copy to .env.staging on the staging server (gitignored).
# Build: npm run build:staging
# Guide: server-environment/staging/RUNBOOK.md

# Same-origin via Nginx (/api, /auth proxied to gateway) — leave empty
VITE_API_BASE_URL=
