/** Max rows returned when loading the full branch invite-link catalog (API cap). */
export const INVITE_LINKS_FULL_LIST_LIMIT = 250;

/** Max rows when filtering invite links via typeahead `q`. */
export const INVITE_LINKS_SEARCH_LIMIT = 100;
