info:
  name: Revoke user sessions (empty body)
  type: http
  seq: 2
  tags:
    - internal

http:
  method: POST
  url: '{{baseUrl}}/internal/users/{{userId}}/sessions/revoke'
  headers:
    - name: Authorization
      value: Bearer {{internalServiceSecret}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |
  Same as **Revoke user sessions** but with **no request body** — idempotent retry / minimal caller shape per OpenAPI (`requestBody` optional).
